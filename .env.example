# ============================================
# director-ai 环境变量入口
# ============================================
#
# 后端配置（必看）：
#   cp backend/.env.example backend/.env
#
# 前端配置（生产构建 / 跨域部署）：
#   cp frontend/.env.example frontend/.env
#   设置 VITE_API_BASE 为后端根地址
#
# 本地开发：backend 默认 :8000，frontend Vite 代理 /api，一般无需改 frontend/.env
#
# 详细说明见 docs/deployment.md
