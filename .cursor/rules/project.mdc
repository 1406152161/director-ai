---
description: director-ai 项目核心约定
alwaysApply: true
---

# director-ai 项目约定

## 项目定位

AI 导演平台：从创意到成片的工业化工作流（Script → Asset → Keyframe → Video）。

## 分支策略

- `main`：稳定发布分支
- `dev`：日常开发分支，功能在此集成后再合并到 main

## 代码规范

- 新增源码文件（Java/Kotlin/Vue/TS/XML 等）类级注释统一使用 `@author zhangzhihao`
- 保持改动最小化，匹配现有代码风格
- 注释只解释非显而易见的业务逻辑

## 技术方向（待实现）

- 前端：React + TypeScript + Vite
- 后端：Python FastAPI 或 Node.js（按模块选型）
- AI 服务：LLM 剧本/分镜、图像生成、视频生成 API 集成

## 目录规划

```
director-ai/
├── frontend/     # 导演工作台 UI
├── backend/      # API 与 Pipeline 编排
├── docs/         # 设计文档
└── .cursor/      # Cursor 规则与配置
```

## Agent 协作分工（强制）

本项目采用「大脑 + 执行者」双角色，**不得混用职责**。

### 主 Agent（默认对话 Agent，即「大脑」）

负责所有**非纯编码**工作，包括但不限于：

- 需求澄清、架构设计、方案权衡、设计文档（`docs/*-design.md`）
- 测试方案与质量保证策略
- **代码审查、验收、回归回测**（必须亲自跑命令验证，不得仅复述子 Agent 报告）
- 向用户汇报结论、风险、实测指引
- 向 Composer 2.5 下发**带完整上下文的实现任务**（含设计文档路径、约束、交付清单）
- Git 审查：`git log` / `git diff`、验收 commit 是否符合设计
- 发现阻塞问题时出修复方案；**简单修复可由大脑直接改**，复杂实现仍派 Composer

**大脑必须执行的回测命令（Composer 交付后）：**

```powershell
cd backend; pytest -q
cd frontend; npm run lint; npm run build
```

必要时补充：针对改动的定向测试、关键路径代码 spot-check、CI 状态核对。

### Composer 2.5（执行者）

**只负责执行**：按设计文档与任务 prompt 写代码、补测试、更新 CHANGELOG、提交并 push。

**Composer 不负责：**

- 架构决策、范围变更、与用户确认需求
- 独立做验收结论或代替大脑回测
- 擅自扩大改动范围或修改 `.github/workflows/`（除非任务明确要求）

Composer 任务结束时应返回：改动摘要、自测结果、commit hash；**最终是否通过以大脑回测与审查为准**。

### 标准流程

```
用户提需求 → 大脑澄清/设计/提问确认 → 大脑写设计文档并提交
→ 大脑派 Composer 实现 → Composer 提交 push
→ 大脑回测 + 审查 + 验收 → 给用户结论与实测指引
→ 有问题：大脑出修复方案 → 派 Composer 或大脑小修 → 再回测
```
