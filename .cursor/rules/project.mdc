---
description: director-ai 项目核心约定
alwaysApply: true
---

# director-ai 项目约定

## 项目定位

AI 导演平台：从创意到成片的工业化工作流（Script → Asset → Keyframe → Video）。

## 分支策略

- `main`：稳定发布分支
- `dev`：日常开发分支，功能在此集成后再合并到 main

## 代码规范

- 新增源码文件（Java/Kotlin/Vue/TS/XML 等）类级注释统一使用 `@author zhangzhihao`
- 保持改动最小化，匹配现有代码风格
- 注释只解释非显而易见的业务逻辑

## 技术栈（已落地）

- 前端：React + TypeScript + Vite（`frontend/`）
- 后端：Python FastAPI + SQLAlchemy（`backend/`）
- 小说线：DeepSeek/智谱 LLM、Chroma 向量记忆、分层规划 M6
- 视频线：Agnes Provider + FFmpeg 合成（mock 可本地开发）
- 执行计划：`docs/execution-plan.md`

## 目录规划

```
director-ai/
├── frontend/     # 导演工作台 UI
├── backend/      # API 与 Pipeline 编排
├── docs/         # 设计文档
└── .cursor/      # Cursor 规则与配置
```

## Agent 协作分工（强制）

本项目采用 **设计 → 执行 Agent → 验收** 三角色流程，细则见 `.cursor/rules/agent-workflow.mdc`。

- **设计**：需求澄清、方案、`docs/*-design.md`，不写业务代码
- **执行 Agent**：按已确认设计写代码、补测试
- **验收**：`pytest` + `npm run build`、审查 diff，给出通过/不通过

用户可用 `【阶段：设计|执行|验收|执行+验收】` 指定当前阶段；信息不足时必须先提问。
