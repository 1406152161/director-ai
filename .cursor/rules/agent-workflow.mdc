---
description: 三角色 Agent 协作流程（设计 / 执行 / 验收）
alwaysApply: true
---

# Agent 协作分工（强制）

本项目采用 **设计 → 执行 Agent → 验收** 三角色，**不得混用职责**。

## 角色

| 角色 | 职责 | 禁止 |
|------|------|------|
| **设计** | 澄清需求、提问、方案权衡、更新 `docs/*-design.md` | 写业务代码、宣布验收通过 |
| **执行 Agent** | 按已确认设计改代码、补测试、更新 CHANGELOG | 改架构/扩范围、代替验收下结论 |
| **验收** | 跑回归、审查 diff、给出通过/不通过 | 大改实现（只报问题与修复建议） |

## 不懂就问

- 需求含糊、存在 2+ 合理方案、涉及 breaking/迁移/默认行为变更、缺少验收标准 → **必须先向用户提问**，不得擅自假设。
- 每轮设计最多问 3 个关键问题；其余可写「默认假设」并标注，供用户纠正。

## 阶段门禁（默认）

**必须走完整三阶段**（设计 → 执行 → 验收）：

- 新功能、新模块、新 API/表结构
- 跨前后端或多文件重构
- 用户消息含 `【阶段：设计】` 或未指定阶段的新需求

**可合并执行 + 验收**（跳过设计，但验收仍必做）：

- 单行/单文件 obvious fix、typo、配置项
- 用户明确说「直接改」「小修」「hotfix」或 `【阶段：执行+验收】`
- 用户已给出完整方案且无需权衡

**禁止**：执行 Agent 自报「验收通过」；以验收角色跑完命令并审查 diff 后的结论为准。

## 验收必跑命令

```powershell
cd backend; python -m pytest -q
cd frontend; npm run build
```

必要时补充：定向 pytest、改动文件 spot-check、CI 状态。

## 标准流程

```
用户提需求
  → 设计：提问 / 方案 / docs → 用户确认（或明确假设）
  → 执行 Agent：按设计实现 → 返回改动摘要 + 自测（非最终验收）
  → 验收：pytest + build + 审查 → 通过 / 不通过
  → 不通过：回执行 Agent 修复 → 再验收
```

## 调用方式

用户可在消息开头指定：`【阶段：设计|执行|验收|执行+验收】`

相关 Skill：`.cursor/skills/director-design`、 `director-implement`、 `director-qa`
